/**
 * @param {number[]} gain
 * @return {number}
 */
var largestAltitude = function(gain) {
    let curr = 0;
    let larAlt = 0;
    for(let i=0; i< gain.length; i++){
        curr += gain[i];
        larAlt = Math.max(larAlt, curr);
    }
    return larAlt;
};
